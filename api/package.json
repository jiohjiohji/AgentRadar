{
  "name": "@agentRadar/api",
  "version": "0.1.0",
  "private": true,
  "description": "AgentRadar Cloudflare Worker — Git-backed JSON API over the YAML dataset",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "js-yaml": "^4.1.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "wrangler": "^3.65.0"
  }
}
